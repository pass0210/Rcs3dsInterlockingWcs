# S-CLEANUP-FIELD — Module 1 (백엔드 관측성) 작업 로그

- 워크트리: `.claude/worktrees/cleanup-m1` (branch `wt/cleanup-m1`, base HEAD 7aab6e3)
- Generator: Module-1. 스코프 = D-1·D-2·D-3·D-4·A-1·A-2 (백엔드 관측성만).
- 핸드셰이크/판정(Decide) 의미 변경 0. frontend·Sim3ds·scripts·docs·기존 테스트 무접촉.

## 변경 요약 (항목별)

### D-1 — OFFLINE 로그 스팸 억제 (`PlcGateway.cs`, `Program.cs`, `appsettings*.json`)
- 폴 실패 로깅을 전이/지속으로 분리:
  - **전이 1회만** 상세(`LogError(ex, …)` — 스택 포함). `PublishOffline()`가 `bool`(Online 1→0 성공=전이) 반환하도록 변경, 전이 폴만 상세 로그.
  - **지속 실패**: 스택 없는 `LogDebug` 강등 + `OfflineLogSummaryEveryPolls`마다 `LogWarning` 요약 1줄(스택 없음). isHardEx가 매 폴 true여도 전이 라벨 반복 안 함(거짓 전이 라벨 제거).
  - **ONLINE 복구**: 성공 전이(false→true) 시 `LogInformation("[폴링] ONLINE — 폴 성공(기동/복구)")` 1회. `_offlineFailureCount`는 정상 폴 성공 시 리셋.
- 신규 타이밍 `OfflineLogSummaryEveryPolls`(기본 20)를 `PlcGatewayOptions`·`SorterTimingOverride`(nullable, 소터별 오버라이드)·`BuildGatewayOptions` 병합·`appsettings.json Timing`에 추가 — **하드코딩 0(절대규칙 #7)**.

### D-2 — Serilog rollOnFileSizeLimit (`appsettings.json`, `appsettings.Development.json`)
- File 싱크 Args에 `rollOnFileSizeLimit: true` + `fileSizeLimitBytes: 104857600`(100MB) 추가(base·dev 둘 다 정합). 기본 1GB 도달 후 그날 잔여 로그 침묵 유실 방지.

### D-3 — /health liveness (`Program.cs`)
- `app.MapGet("/health", …)` 신설. `MapControllers` 뒤·`/api` catch-all/fallback 앞(삼킴 방지).
- 응답: `{status:"ok", db:bool, sorters:[{chuteNo, online, lastPollAt}]}`. **항상 200**(Q2 확정 — readiness 503은 B2B-1 이연). 전용 HealthChecks 프레임워크 미도입(단일 엔드포인트, 과설계 0).
- 부수효과 0: `ISorterGatewayRegistry.AllBundles`의 `Latest.Online/At` 스냅샷 읽기 + `db.Database.CanConnect()`(읽기 전용). 쓰기·상태전이 없음. CanConnect 예외도 liveness 200 유지(db=false로만 표시).

### D-4 — 입력 상한 (`RcsController.cs`, `WcsOptions.cs`, `appsettings.json`)
- 위반 시 **400 Bad Request**(기존 IF-05 검증 관행 `BadRequest(new { error })` 동형), DB 도달 전 거부. 정상 입력 경로 불변.
  - barcode 길이 ≤ 200(IF-05·IF-10) — 상수 `BarcodeMaxLength`(WcsDbContext nvarchar(200) 스키마 미러, 주석 명시).
  - timeStamp 길이 ≤ 30(IF-05·IF-09·IF-10) — 상수 `TimeStampMaxLength`(ClientTs nvarchar(30) 미러). ClientTs 절단 500 방지.
  - IF-05 qty 상한(설정값 `WcsOptions.MaxQtyPerRequest`, 기본 100000) — `int.MaxValue` 등 비정상 대량 qty를 DB 도달 전 거부 → OVER 검사 int 오버플로 우회 원천 차단(qty가 상한으로 묶이므로 `ReservedQty + qty` 안전). DbRepositories 미수정(스코프 밖·불요).
  - IF-10 qty(nullable) 음수 거부 + 상한 초과 거부.
- **파일 경계 메모**: `WcsOptions.cs`는 Module 1 파일 목록에 명시 안 됐으나 qty 상한 설정화(절대규칙 #7)의 자연스러운 자리이며 Module 2/3 쓰기 파일과 무충돌. 신규 옵션 섹션 추가(과확장) 대신 기존 `WcsOptions`에 필드 1개 추가(최소 침습·Consistency).

### A-1 — HS_R_RESIDUE 레벨 승격 (`Program.cs`)
- 인라인 삼항 레벨 분류기를 `public static class OperationLogClassifier.ForHandshakeStage(string)`로 추출(단일 진실·테스트 가능). `SubscribeHandshakeStage` 구독부가 위임.
- 분류: MISMATCH/TIMEOUT/OFFLINE → ERROR(HS_R_RESIDUE_TIMEOUT은 TIMEOUT 포함 → ERROR 유지) / **RESIDUE → WARN**(HS_R_RESIDUE 잔류 감지 승격, INFO 매몰 해소) / 그 외 → INFO.
- 참고: 테스트 팩토리는 `SorterRegistryFactory`(handshake→opLog 구독 배선)를 `NopSorterRegistryFactory`로 교체하므로 fake 호스트에선 HANDSHAKE operation_log 미배선. 따라서 A-1 검증은 **레벨을 실제로 결정하는 분류기 함수**를 단위 테스트(운영 배선이 이 레벨을 그대로 opLog.Log에 전달).

### A-2 — 기동 reconcile spurious RFlagRaised 에지 억제 + F-3 주석 (`PlcGateway.cs`)
- 기동 첫 폴 잔류 R_Flag=1 reconcile(ClearR 큐 투입) 시 같은 폴의 `RFlagRaised` 상승 에지 발화를 `suppressRFlagEdge`로 억제(지울 잔류값을 에지로 흘리지 않음).
- F-3: `RFlagRaised` 채널 **소비자 부재** 상태 주석 명시 — HandshakeOrchestrator는 `ArmRFlagZeroAsync`에서 `_gw.Latest` 직접 폴링으로 R_Flag를 감지하며 이 reader를 구독하지 않음(향후 이벤트 구동 소비자용 후크). 억제/게시는 핸드셰이크 의미에 영향 0.

## 검증 (fresh 결과)
- `dotnet build backend/Wcs.sln`: **오류 0**, 경고 10(전부 선재 NU1903 SQLitePCLRaw advisory — todo:4, 신규 경고 0).
- 표적 `--filter FullyQualifiedName~CleanupFieldM1`: **통과 19 / 실패 0** (D-1 로그억제 1, A-1 분류기 12(theory 11+1), D-3 health 1, D-4 caps 5).
- 영향권(무변경 가드):
  - PlcGatewayIntegrationTests + ApiIntegrationTests + HandshakeResidueTests: **통과 39 / 0**.
  - SorterPushOperational + SorterCellFullness + MonitorHub + MonitoringApi + RcsPush + DbSeedGate + Scenario: **통과 56 / 0**.
  - Field20CellsGateTests(호스트 기동 확인): **통과 3 / 0**.
- 전체 스위트(189 baseline)·E2E는 fan-in에서 통합 실행(계약 §Parallel Modules).
- 종료 전 orphan testhost/Wcs.Sim3ds/vstest 프로세스: 없음.

## Fix — fan-in 통합 병렬 DB 격리 (2026-07-07, MAIN repo `fix/cleanup-field`)

**증상**: 표적 실행은 GREEN이었으나 fan-in 후 전체 스위트 210 중 5 FAIL —
`ApiIntegrationTests.If05_Chute_Full_ThenCleared_Normal`(`SqliteException: table "agv" already exists`),
`VS6_If10_3dDestination_TriggersHandshakeCFlag`(`UNIQUE … destination.ChuteNo`),
`If09_InvalidPId_Returns400`(`UNIQUE … cell.DestinationId,cell.CellNo`),
`CleanupFieldM1_HttpTests.D4_If05_BarcodeTooLong_Returns400`(`UNIQUE … work_batch.WorkDate`) 외 1건.

**근본 원인**: `FakeModbusWebApplicationFactory._dbName`(ApiIntegrationTests.cs:55)이 **`static readonly`**라
그 팩토리 타입의 모든 인스턴스가 같은 named in-memory SQLite DB(`WcsTest_<guid>`, `Mode=Memory;Cache=Shared`)를 공유한다.
`ApiIntegrationTests` 단일 클래스만 쓸 땐(IClassFixture=인스턴스 1개) 무해했으나, 신규
`CleanupFieldM1_HttpTests`가 **두 번째** `IClassFixture<FakeModbusWebApplicationFactory>`를 추가하며 두 클래스
픽스처가 같은 static DB 이름으로 해석 → xUnit 클래스 병렬 실행 시 두 인스턴스가 같은 DB에 `EnsureCreated()`+
`DbSeeder.Seed()`를 이중 수행 → 위 스키마/UNIQUE 충돌. (기존 관행: Hub·Monitoring·RcsPush·Scenario·S8·E2E
팩토리는 모두 **인스턴스** `_dbName = $"…_{Guid.NewGuid():N}"`로 자체 격리 — Fake만 static 예외였음.)

**수정(신규 파일 한정)**: `CleanupFieldM1Tests.cs`에 전용 `CleanupFieldM1HttpWebApplicationFactory` 추가 —
`HubWebApplicationFactory` 패턴과 동형으로 **인스턴스-고유 `_dbName = $"CleanupM1Test_{Guid.NewGuid():N}"`**.
배선(폴링·소터 레지스트리·시드·`IAsyncLifetime` teardown)은 기존 팩토리와 동일하되 DB 이름만 인스턴스 분리.
공개 헬퍼(`FakeModbusMasterForApi`·`NopSorterRegistryFactory`·`FakeSorterGatewayRegistry`) 재사용.
`CleanupFieldM1_HttpTests`의 IClassFixture 대상을 새 팩토리로 교체. **기존 테스트 파일·프로덕션 코드 무수정**
(결함은 신규 파일이 static-DB 팩토리를 이중 사용한 것 → 신규 팩토리 격리로 해소).

**검증(fresh)**: `dotnet build` 오류 0. 전체 스위트 `dotnet test backend/Wcs.sln` **3회 연속 210/210 GREEN**
(실패 0 · 13s/14s/14s). 병렬 충돌 재발 0, 선재 flake(F1b) 미발현. 종료 후 orphan testhost/Sim3ds/vstest 없음.

## D-1 로그 캡처 근거 (표적 테스트 단정)
`D1_SustainedOffline_SuppressesLogSpam_OneTransitionAndRecovery`:
- SetFailReads(true)로 지속 OFFLINE 유도, 지속 로그 라인 ≥ 12(폴 다수 경과) 관측 시점에:
  - "OFFLINE 전이" ERROR 라인 **== 1**, 총 ERROR **== 1**, 예외(스택) 첨부 라인 **== 1** → 폴마다 스택 반복 아님(스팸 억제).
  - 요약 WARN("OFFLINE 지속 — 누적") ≥ 1(주기 요약 동작).
- 복구 시 "ONLINE" INFO **정확히 1회 증가**.
