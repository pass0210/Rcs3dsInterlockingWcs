# Sprint S-CLEANUP-FIELD — Module 3 (문서 현행화) 작업 로그

- 워크트리: `.claude/worktrees/cleanup-m3` (브랜치 `wt/cleanup-m3`, base HEAD 7aab6e3)
- 담당: Generator Module-3
- 스코프: 문서만 (README.md · wcs_rcs_3ds_master_spec.html · SPEC.md). CLAUDE.md 무접촉(오케스트레이터 적용). backend/frontend/scripts 코드 무수정.

## 구현 항목

### A-5 — master_spec.html §05 FULL/PAUSED 타입별 분기 현행화
- **문제**: §05가 "FULL·PAUSED는 NG"(타입 무구분)로, 확정4·실코드(`RcsController.QueryDestination` + `DbRepositories`)·`interface_kr.html §6`과 충돌.
- **정정(실코드 확인 후)**: IF-05에서 **슈트=OK**(보내고 대기, readiness는 IF-08 푸시로 별도 전달), **3D 소터**=PAUSED면 NG·`SorterCanAcceptBarcode`로 못 받으면 NG(FULL)·OFFLINE은 IF-05에서 안 봄.
- 수정 위치 3곳: §05 도입 `<p>`, FULL/PAUSED 표 행, 하단 note(+ interface_kr·RcsController 정합 참조 추가).
- 근거 코드: `RcsController.cs:62-85`(availability 델리게이트 — 슈트=None 통과, 소터=Paused/SorterCanAcceptBarcode), `DbRepositories.cs:68-135`.

### A-20 — README.md 전면 현행 재작성
- 폐지/표류 제거·현행 반영:
  - 다이어그램 `Modbus TCP` → `Modbus RTU/TCP`.
  - IF-08 폴링('allowed=true까지 폴링') → **WCS→RCS 상태 푸시**. IF-09 도착 보고 신설 반영. 엔드포인트 실경로 명시(destination-query/arrival-report/deposit-report).
  - `Minimal API` → **ASP.NET Core MVC**(Controllers) — 실코드(`AddControllers`/`MapControllers`) 확인.
  - 솔루션 구조에 `Wcs.Migrations.SqlServer`·`Wcs.Migrations.Sqlite`·`frontend/` 추가. 모니터링 API(`/api/monitor/*`)·SignalR(`/hubs/monitor`) 반영.
  - DB: "개발은 SQLite 분기" → 운영 SQL Server / 개발·테스트 SQLite(provider별 마이그레이션 분리).
  - Serilog "M5 도입 예정"(미래형) → 현행(콘솔·파일 + operation_log).
  - ERD 16테이블 → **17테이블**.
  - 실행법: 백엔드(API :5080·Sim3ds TCP :1502 기본·RTU 옵션 `--transport rtu --port COMx`) + 프론트엔드(`npm run dev` :5173 프록시, `npm run build`→wwwroot).
  - 문서 링크: 현존 5 HTML + SPEC/ERD/FRONTEND/RTU-REHEARSAL 마크다운으로 갱신(영문 원본·DESIGN-airbnb 언급).
  - 로드맵(미래형 M0~M5 표) 제거 → TASKS.md 이력 포인터로 대체.
- 검증: 실코드·appsettings에서 포트·경로·전송·프레임워크·테이블수 전수 확인. 모든 링크 대상 파일 존재 확인.

### E-SPEC — SPEC.md §2/§3 IF-08 푸시모델 명료화(최소, 판정표 보존)
- §2 상단에 노트 1개: IF-08 `deposit-permission`(RCS 폴링) **폐지 → WCS→RCS 푸시 대체**(interface_kr §6·master_spec §06). **판정표 2-A/2-B는 내부 `DepositDecider` 스펙으로 유효 — 삭제 안 함**(소터 push ready·IF-09 정렬 근거).
- §3 `deposit-permission` bullet: 폐지 마킹 + 옛 응답 형상은 이제 RCS로 안 나가고 내부 판정 결과임 명시. IF-09 신설(arrival-report) 언급.
- 판정표(§2-A/2-B)·핸드셰이크(§4)·미확정(§7) 전부 보존.

## 검증
- `dotnet build backend/Wcs.sln` → exit 0(문서 변경이라 코드 무영향, 명령 유효성 확인).
- `git status --short`: `M README.md` / `M docs/SPEC.md` / `M docs/wcs_rcs_3ds_master_spec.html` — 정확히 3개 문서만. CLAUDE.md·backend·frontend·scripts 무수정.
- README 모든 링크 대상 파일 존재 확인(docs 10종·TASKS.md·CLAUDE.md·scripts 3종).

## 스코프 밖(무접촉 준수)
- CLAUDE.md(A-6): 보호파일 — 오케스트레이터 적용. 미수정.
- backend/src·tests·scripts·frontend 코드: 읽기만, 무수정.
