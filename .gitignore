bin/
obj/
*.user
.vs/
appsettings.*.local.json
logs/
.claude/ports.local.json
.env.local
*.db
*.db-shm
*.db-wal
TestResults/

# dotnet publish 산출물(배포 게시물 — DEPLOY-AZURE-RCS-TEST.md §A 참조, 소스 아님)
publish/

# ── 프론트엔드(F1) — Node 툴체인 + 빌드 산출물(소스만 커밋) ──
frontend/node_modules/
frontend/dist/
# npm run build 산출물(vite build.outDir) — Wcs.Api 정적 서빙 대상, 소스 아님
backend/src/Wcs.Api/wwwroot/

# 평가 산출물(스크린샷 증거 — 커밋 안 함)
screenshots/
portfolio-screenshots/
tasks/evidence/
.claude/

# Playwright MCP 세션 잔여물
.playwright-mcp/
# MCP 설정 — 개발자별 로컬(커밋 안 함)
.mcp.json

# 협력사(비에텔) RCS-WES API 문서 — 벤더 프로토콜 원본 + 우리 요청서(로컬 전용·커밋 안 함)
docs/RCS-WES-API-KR.md
docs/RCS-WES-API-REQUEST-KR.md
